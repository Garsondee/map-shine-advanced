/**
 * Stub: Tweakpane is not used by the V3 minimal build. This file exists so
 * older module.json manifests that still list this path do not 404 (HTML
 * error pages break ES module loading with MIME / nosniff errors).
 */
export {};
