# THE PRESS — moved

This document now lives in `Mythica Machina Press/docs/planning/The-Press-Module-Release-Pipeline-2026-08-31.md`, alongside the module catalogue it describes rather than inside the engine repo.

See memory `project_module_release_pipeline_the_press` for the current status.
