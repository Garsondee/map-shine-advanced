/**
 * MACHINE-HARVESTED from legacy/compositor-v2/effects/BloomEffectV2.js (V2 class BloomEffectV2), 2026-07-17.
 * Generated by tools/harvest-params.mjs — see that file for the transform rules.
 * Do not hand-edit the `SCHEMA`/`GROUPS` exports; re-run the harvest instead.
 */
export const SOURCE_CLASS = 'BloomEffectV2';
export const SOURCE_FILE = 'legacy/compositor-v2/effects/BloomEffectV2.js';
export const GROUPS = [
  {
    name: 'look',
    label: 'Surface',
    type: 'folder',
    expanded: true,
    parameters: ['strength', 'radius', 'threshold'],
  },
  {
    name: 'atmosphere',
    label: 'Atmosphere',
    type: 'folder',
    expanded: true,
    parameters: ['atmoEnabled', 'atmoStrength', 'atmoRadius', 'atmoTintColor', 'atmoBlendOpacity'],
  },
  {
    name: 'water-spec-bloom',
    label: 'Water specular (bloom)',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: ['waterSpecularBloomEnabled', 'waterSpecularBloomStrength', 'waterSpecularBloomGamma'],
  },
  {
    name: 'grade',
    label: 'Surface grade',
    type: 'folder',
    expanded: true,
    parameters: ['tintColor', 'blendOpacity'],
  },
  {
    name: 'lightning-strike',
    label: 'Lightning strike',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'lightningBloomAdaptEnabled',
      'lightningBloomThresholdBoost',
      'lightningBloomStrengthMul',
      'lightningBloomRadiusMul',
      'lightningBloomSmoothWidth',
      'lightningBloomBlendMul',
      'lightningBloomPassthroughPeak',
      'lightningBloomMapPointWeight',
    ],
  },
  {
    name: 'outdoor-spill',
    label: 'Outdoor spill (window glow)',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: ['outdoorSpillSuppressEnabled', 'outdoorSpillLumLoMul', 'outdoorSpillLumHiMul'],
  },
  {
    name: 'fog-clip',
    label: 'Fog clip (vision)',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: ['fogClipEnabled'],
  },
];
export const SCHEMA = {
  enabled: {
    type: 'bool',
    default: true,
    label: 'enabled',
  },
  strength: {
    type: 'float',
    default: 1.08,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Surface strength',
    help: 'Tight surface glow on bright pixels (hot source / specular).',
  },
  radius: {
    type: 'float',
    default: 1,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Surface radius',
    help: 'Spread of the tight surface halo (0–2).',
  },
  threshold: {
    type: 'float',
    default: 3.01,
    min: 0,
    max: 4,
    step: 0.01,
    label: 'Threshold',
    help: 'Linear brightness floor; only brighter pixels bloom. With the Linear HDR pipeline the merged scene can exceed 1.0, so this range was extended — 1.0 means "only true highlights bloom".',
  },
  tintColor: {
    type: 'color',
    default: '#ffffff',
    space: 'linear',
    label: 'Surface tint',
    help: 'Tint on the tight surface bloom.',
  },
  blendOpacity: {
    type: 'float',
    default: 1,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Surface blend',
    help: 'Mix for the surface bloom layer.',
  },
  atmoEnabled: {
    type: 'bool',
    default: true,
    label: 'Atmosphere enabled',
    help: 'Wide secondary bloom from the same bright pixels — air catching surface light.',
  },
  atmoStrength: {
    type: 'float',
    default: 0.42,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Atmosphere strength',
    help: 'Intensity of the wide atmospheric scatter.',
  },
  atmoRadius: {
    type: 'float',
    default: 3.5,
    min: 0,
    max: 12,
    step: 0.05,
    label: 'Atmosphere radius',
    help: 'Atmospheric spread (0–12). Much wider than surface — haze and air glow.',
  },
  atmoTintColor: {
    type: 'color',
    default: '#ffebc7',
    space: 'linear',
    label: 'Atmosphere tint',
    help: 'Tint on the atmospheric layer (warm haze by default).',
  },
  atmoBlendOpacity: {
    type: 'float',
    default: 0.72,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Atmosphere blend',
    help: 'Mix for the atmospheric bloom layer.',
  },
  waterSpecularBloomEnabled: {
    type: 'bool',
    default: true,
    label: 'Link water specular',
    help: 'When on (and water renders a mask), add water specular energy into the bloom input before threshold.',
  },
  waterSpecularBloomStrength: {
    type: 'float',
    default: 8,
    min: 0,
    max: 8,
    step: 0.01,
    label: 'Water bloom strength',
    help: 'Linear HDR added from the water specular mask. Push high for aggressive sun glints.',
  },
  waterSpecularBloomGamma: {
    type: 'float',
    default: 0.81,
    min: 0.35,
    max: 3,
    step: 0.01,
    label: 'Water bloom gamma',
    help: 'Shapes the injected mask before bloom (1 = linear). Lower emphasizes peaks.',
  },
  lightningBloomAdaptEnabled: {
    type: 'bool',
    default: true,
    label: 'Adapt during strikes',
    help: 'Raise bloom cutoff and soften the high-pass knee while lightning flashes are active to avoid banded halos.',
  },
  lightningBloomThresholdBoost: {
    type: 'float',
    default: 2,
    min: 0,
    max: 4,
    step: 0.05,
    label: 'Strike threshold boost',
    help: 'Added to the linear threshold at full strike intensity — keeps broad flash wash out of bloom.',
  },
  lightningBloomStrengthMul: {
    type: 'float',
    default: 0.3,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Strike strength mul',
    help: 'Bloom strength multiplier when a strike is at peak (flash already adds glow).',
  },
  lightningBloomRadiusMul: {
    type: 'float',
    default: 0.55,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Strike radius mul',
    help: 'Bloom spread multiplier at full strike intensity — tighter blur reduces banding.',
  },
  lightningBloomSmoothWidth: {
    type: 'float',
    default: 0.45,
    min: 0.05,
    max: 1.5,
    step: 0.01,
    label: 'Strike smooth width',
    help: 'High-pass knee width during strikes (wider = softer cutoff, fewer bands).',
  },
  lightningBloomBlendMul: {
    type: 'float',
    default: 0.65,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Strike blend mul',
    help: 'Blend opacity multiplier at full strike intensity.',
  },
  lightningBloomPassthroughPeak: {
    type: 'float',
    default: 0.88,
    min: 0.5,
    max: 1,
    step: 0.01,
    label: 'Passthrough peak',
    help: 'Skip bloom entirely above this strike weight — useful for the brightest flash peak.',
  },
  lightningBloomMapPointWeight: {
    type: 'float',
    default: 0.15,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Map-point adapt weight',
    help: 'How much localized arc flashes affect adaptation (lower preserves arc bloom).',
  },
  outdoorSpillSuppressEnabled: {
    type: 'bool',
    default: true,
    label: 'Suppress outdoor spill',
    help: 'Remove bloom on dark outdoor pixels (_Outdoors) so indoor window glow does not halo onto surrounding ground.',
  },
  outdoorSpillLumLoMul: {
    type: 'float',
    default: 0.42,
    min: 0.05,
    max: 1.5,
    step: 0.01,
    label: 'Spill lum lo (× threshold)',
    help: 'Outdoor pixels below threshold × this lose spilled bloom entirely.',
  },
  outdoorSpillLumHiMul: {
    type: 'float',
    default: 0.92,
    min: 0.1,
    max: 2,
    step: 0.01,
    label: 'Spill lum hi (× threshold)',
    help: 'Outdoor pixels above threshold × this keep full bloom (sun, torches, water glints).',
  },
  fogClipEnabled: {
    type: 'bool',
    default: true,
    label: 'Clip to vision (FoW)',
    help: 'Mask bloom source pixels to current token LOS before blur — stops distant in-fog glow from leaking into seen areas.',
  },
};
