/**
 * MACHINE-HARVESTED from legacy/compositor-v2/effects/BuildingShadowsEffectV2.js (V2 class BuildingShadowsEffectV2), 2026-07-17.
 * Generated by tools/harvest-params.mjs — see that file for the transform rules.
 * Do not hand-edit the `SCHEMA`/`GROUPS` exports; re-run the harvest instead.
 */
export const SOURCE_CLASS = 'BuildingShadowsEffectV2';
export const SOURCE_FILE = 'legacy/compositor-v2/effects/BuildingShadowsEffectV2.js';
export const GROUPS = [
  {
    name: 'mask-status-outdoors',
    type: 'mask-status',
    parameters: [],
  },
  {
    name: 'main',
    label: 'Building Shadows',
    type: 'inline',
    parameters: [
      'opacity',
      'shadowStrengthBoost',
      'length',
      'softness',
      'smear',
      'resolutionScale',
      'penumbra',
      'shadowCurve',
      'blurRadius',
      'contactShadowPreserve',
      'contactSharpBlendLow',
      'contactSharpBlendHigh',
      'shadowEdgeInflatePx',
    ],
  },
];
export const SCHEMA = {
  opacity: {
    type: 'float',
    default: 0.9,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Opacity',
  },
  shadowStrengthBoost: {
    type: 'float',
    default: 1.3,
    min: 1,
    max: 10,
    step: 0.05,
    label: 'Strength boost',
    help: 'Extra darkening beyond opacity (×1 matches older behavior; use up to ×10 when shadows look too faint).',
  },
  length: {
    type: 'float',
    default: 0.1,
    min: 0,
    max: 0.6,
    step: 0.005,
    label: 'Length',
    help: 'Peak shadow length at dawn/dusk (~400 px at default). Scales toward zero at solar noon and midnight.',
  },
  softness: {
    type: 'float',
    default: 0.5,
    min: 0.5,
    max: 8,
    step: 0.1,
    label: 'Softness',
    help: 'Lateral spread per ray step; grows toward the shadow tip for a softer umbra away from walls.',
  },
  smear: {
    type: 'float',
    default: 0.5,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Smear',
    help: 'Stretches and softens the shadow tail along the sun direction (higher = more smeared, painterly falloff).',
  },
  resolutionScale: {
    type: 'float',
    default: 1,
    min: 1,
    max: 2,
    step: 0.05,
    label: 'Resolution',
  },
  penumbra: {
    type: 'float',
    default: 0.11,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Penumbra',
    help: 'How quickly the shadow softens and lightens along its length (away from the building).',
  },
  shadowCurve: {
    type: 'float',
    default: 1.6,
    min: 0.5,
    max: 1.6,
    step: 0.01,
    label: 'Shadow Curve',
    help: 'Gamma on integrated shadow strength; lower = gentler fade into light.',
  },
  blurRadius: {
    type: 'float',
    default: 4,
    min: 0,
    max: 4,
    step: 0.05,
    label: 'Blur',
  },
  contactShadowPreserve: {
    type: 'float',
    default: 1,
    min: 0,
    max: 1,
    step: 0.02,
    label: 'Contact preserve',
    help: 'Blurs outward without eating the caster edge: merges pre-blur strength where the footprint is darkest, full blur where it fades. Lower = softer contact.',
  },
  contactSharpBlendLow: {
    type: 'float',
    default: 0,
    min: 0,
    max: 0.35,
    step: 0.005,
    label: 'Contact blend (low)',
    help: 'Lower bound for where pre-blur strength starts to dominate (shadow strength gamma). Raise slightly if fringe looks too crunchy.',
  },
  contactSharpBlendHigh: {
    type: 'float',
    default: 0.98,
    min: 0.2,
    max: 0.98,
    step: 0.01,
    label: 'Contact blend (high)',
    help: 'Upper bound toward full contact sharpness inside the silhouette. Lower to pull softness closer to walls.',
  },
  shadowEdgeInflatePx: {
    type: 'float',
    default: 0,
    min: 0,
    max: 8,
    step: 0.05,
    label: 'Edge inflate (px)',
    help: 'Expands shadow strength slightly in the shadow buffer (in RT pixels) so coverage tucks under the footprint and hides bright rim lines. 0 = off.',
  },
};
