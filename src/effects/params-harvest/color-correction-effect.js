/**
 * MACHINE-HARVESTED from legacy/compositor-v2/effects/ColorCorrectionEffectV2.js (V2 class ColorCorrectionEffectV2), 2026-07-17.
 * Generated by tools/harvest-params.mjs — see that file for the transform rules.
 * Do not hand-edit the `SCHEMA`/`GROUPS` exports; re-run the harvest instead.
 */
export const SOURCE_CLASS = 'ColorCorrectionEffectV2';
export const SOURCE_FILE = 'legacy/compositor-v2/effects/ColorCorrectionEffectV2.js';
export const GROUPS = [
  {
    name: 'mask-status-outdoors',
    type: 'mask-status',
    parameters: [],
  },
  {
    name: 'exposure-color',
    label: 'Exposure & color',
    type: 'folder',
    expanded: true,
    parameters: ['exposure', 'temperature', 'tint', 'contrast', 'brightness', 'saturation', 'vibrance'],
  },
  {
    name: 'outdoor-atmosphere',
    label: 'Outdoor atmosphere',
    type: 'folder',
    expanded: true,
    parameters: [
      'atmosphereEnabled',
      'intensity',
      'saturationBoost',
      'vibranceBoost',
      'shadowGradePreserve',
      'calendarDarknessBlend',
      'dayNightGradePull',
      'nightExtraDarkness',
      'autoIntensityEnabled',
      'autoIntensityStrength',
      'sunriseHour',
      'sunsetHour',
      'goldenHourWidth',
      'goldenStrength',
      'goldenPower',
      'goldenOutdoorRecolorStrength',
      'goldenOutdoorRecolorColor',
      'nightFloor',
      'analyticStrength',
      'turbidity',
      'rayleighStrength',
      'mieStrength',
      'forwardScatter',
      'weatherInfluence',
      'cloudToTurbidity',
      'precipToTurbidity',
      'overcastDesaturate',
      'overcastContrastReduce',
      'tempWarmAtHorizon',
      'tempCoolAtNoon',
      'nightCoolBoost',
      'goldenSaturationBoost',
      'nightSaturationFloor',
      'hazeLift',
      'hazeContrastLoss',
    ],
  },
  {
    name: 'grading',
    label: 'HDR tone mapping',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: ['toneMapping', 'liftColor', 'gammaColor', 'gainColor', 'masterGamma'],
  },
  {
    name: 'artistic',
    label: 'Vignette & grain',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: ['vignetteStrength', 'vignetteSoftness', 'grainStrength'],
  },
  {
    name: 'tod-timeline',
    label: 'Time-of-day camera timeline',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'todTimelineEnabled',
      'localWarmLightPreserve',
      'localTodOverrideExposure',
      'localTodOverrideSaturation',
      'localWarmEmissiveAdd',
      'lampLightPreserve',
    ],
  },
  {
    name: 'tod-anchor-4',
    label: 'Noon (~12:00)',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'tod4Hour',
      'tod4GlobalExposure',
      'tod4GlobalSaturation',
      'tod4GlobalTintR',
      'tod4GlobalTintG',
      'tod4GlobalTintB',
      'tod4InteriorExposure',
      'tod4InteriorSaturation',
      'tod4InteriorTintR',
      'tod4InteriorTintG',
      'tod4InteriorTintB',
    ],
  },
  {
    name: 'tod-anchor-5',
    label: 'Afternoon (~15:00)',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'tod5Hour',
      'tod5GlobalExposure',
      'tod5GlobalSaturation',
      'tod5GlobalTintR',
      'tod5GlobalTintG',
      'tod5GlobalTintB',
      'tod5InteriorExposure',
      'tod5InteriorSaturation',
      'tod5InteriorTintR',
      'tod5InteriorTintG',
      'tod5InteriorTintB',
    ],
  },
  {
    name: 'tod-anchor-6',
    label: 'Dusk (~18:00)',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'tod6Hour',
      'tod6GlobalExposure',
      'tod6GlobalSaturation',
      'tod6GlobalTintR',
      'tod6GlobalTintG',
      'tod6GlobalTintB',
      'tod6InteriorExposure',
      'tod6InteriorSaturation',
      'tod6InteriorTintR',
      'tod6InteriorTintG',
      'tod6InteriorTintB',
    ],
  },
  {
    name: 'tod-anchor-7',
    label: 'Night (~21:00)',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'tod7Hour',
      'tod7GlobalExposure',
      'tod7GlobalSaturation',
      'tod7GlobalTintR',
      'tod7GlobalTintG',
      'tod7GlobalTintB',
      'tod7InteriorExposure',
      'tod7InteriorSaturation',
      'tod7InteriorTintR',
      'tod7InteriorTintG',
      'tod7InteriorTintB',
    ],
  },
  {
    name: 'tod-anchor-0',
    label: 'Midnight (~00:00)',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'tod0Hour',
      'tod0GlobalExposure',
      'tod0GlobalSaturation',
      'tod0GlobalTintR',
      'tod0GlobalTintG',
      'tod0GlobalTintB',
      'tod0InteriorExposure',
      'tod0InteriorSaturation',
      'tod0InteriorTintR',
      'tod0InteriorTintG',
      'tod0InteriorTintB',
    ],
  },
  {
    name: 'tod-anchor-1',
    label: 'Pre-dawn (~03:00)',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'tod1Hour',
      'tod1GlobalExposure',
      'tod1GlobalSaturation',
      'tod1GlobalTintR',
      'tod1GlobalTintG',
      'tod1GlobalTintB',
      'tod1InteriorExposure',
      'tod1InteriorSaturation',
      'tod1InteriorTintR',
      'tod1InteriorTintG',
      'tod1InteriorTintB',
    ],
  },
  {
    name: 'tod-anchor-2',
    label: 'Dawn (~06:00)',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'tod2Hour',
      'tod2GlobalExposure',
      'tod2GlobalSaturation',
      'tod2GlobalTintR',
      'tod2GlobalTintG',
      'tod2GlobalTintB',
      'tod2InteriorExposure',
      'tod2InteriorSaturation',
      'tod2InteriorTintR',
      'tod2InteriorTintG',
      'tod2InteriorTintB',
    ],
  },
  {
    name: 'tod-anchor-3',
    label: 'Morning (~09:00)',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'tod3Hour',
      'tod3GlobalExposure',
      'tod3GlobalSaturation',
      'tod3GlobalTintR',
      'tod3GlobalTintG',
      'tod3GlobalTintB',
      'tod3InteriorExposure',
      'tod3InteriorSaturation',
      'tod3InteriorTintR',
      'tod3InteriorTintG',
      'tod3InteriorTintB',
    ],
  },
];
export const SCHEMA = {
  enabled: {
    type: 'bool',
    default: true,
    label: 'enabled',
  },
  todTimelineEnabled: {
    type: 'bool',
    default: true,
    label: 'Enable time-of-day timeline',
    help: 'Blends eight clock anchors (global + interior grades) as Map Shine time advances. This is the camera-grade owner for visible time-of-day exposure.',
  },
  localWarmLightPreserve: {
    type: 'float',
    default: 1,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Local ToD Override Strength',
    help: 'Blends from the scene timeline grade toward a bright neutral local grade under gameplay lights (HDR light-buffer alpha). 0 = full midnight/global tint everywhere; 1 = full local override inside lit pools only.',
  },
  localTodOverrideExposure: {
    type: 'float',
    default: 1,
    min: -2,
    max: 5,
    step: 0.05,
    label: 'Local Override Exposure',
    help: 'Base exposure stops added inside local-light pools on top of the timeline grade. Stronger lights gain extra stops automatically — counters midnight darkness and blue tint.',
  },
  localTodOverrideSaturation: {
    type: 'float',
    default: 1,
    min: 0.5,
    max: 2,
    step: 0.01,
    label: 'Local Override Saturation',
    help: 'Minimum saturation multiplier inside local-light pools when overriding toward neutral tint. Brighter pool cores push slightly higher.',
  },
  localWarmEmissiveAdd: {
    type: 'float',
    default: 1,
    min: 0,
    max: 1.5,
    step: 0.01,
    label: 'Fire / Emissive Preserve',
    help: 'Skips time-of-day and atmosphere darkening on HDR flames, sparks, and other emissive art (1 = full preserve). Base exposure/WB still apply.',
  },
  lampLightPreserve: {
    type: 'float',
    default: 0.9,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Lamp Light Preserve',
    help: 'Foundry AmbientLight pools resist interior timeline / context darkening so 0.5 vs 1.0 luminosity stays visible indoors.',
  },
  tod4GlobalTintR: {
    type: 'float',
    default: 0.9,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint R',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod4GlobalTintG: {
    type: 'float',
    default: 0.9,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint G',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod4GlobalTintB: {
    type: 'float',
    default: 1.13,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint B',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod4InteriorTintR: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint R',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod4InteriorTintG: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint G',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod4InteriorTintB: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint B',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod4Hour: {
    type: 'float',
    default: 12,
    min: 0,
    max: 24,
    step: 0.05,
    label: 'Clock hour',
    help: 'When the scene clock is near this anchor (12:00 by default). Anchors blend smoothly around the 24h cycle, including across midnight.',
  },
  tod4GlobalExposure: {
    type: 'float',
    default: 0.7,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Global exposure',
    help: 'Exposure in stops for outdoor and as the base for indoor pixels. Keep within +/-1 for natural looks; +/-3 is extreme.',
  },
  tod4GlobalSaturation: {
    type: 'float',
    default: 1.2,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Global saturation',
    help: 'Global saturation multiplier for this time anchor.',
  },
  tod4InteriorExposure: {
    type: 'float',
    default: -1,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Interior exposure offset',
    help: 'Extra exposure stops added to the global value on indoor pixels only. Use small positive values to keep interiors playable at night.',
  },
  tod4InteriorSaturation: {
    type: 'float',
    default: 1,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Interior saturation',
    help: 'Interior-only saturation multiplier for this time anchor.',
  },
  tod5GlobalTintR: {
    type: 'float',
    default: 1.15,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint R',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod5GlobalTintG: {
    type: 'float',
    default: 0.98,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint G',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod5GlobalTintB: {
    type: 'float',
    default: 1.04,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint B',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod5InteriorTintR: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint R',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod5InteriorTintG: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint G',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod5InteriorTintB: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint B',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod5Hour: {
    type: 'float',
    default: 15,
    min: 0,
    max: 24,
    step: 0.05,
    label: 'Clock hour',
    help: 'When the scene clock is near this anchor (15:00 by default). Anchors blend smoothly around the 24h cycle, including across midnight.',
  },
  tod5GlobalExposure: {
    type: 'float',
    default: 0.9,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Global exposure',
    help: 'Exposure in stops for outdoor and as the base for indoor pixels. Keep within +/-1 for natural looks; +/-3 is extreme.',
  },
  tod5GlobalSaturation: {
    type: 'float',
    default: 1.08,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Global saturation',
    help: 'Global saturation multiplier for this time anchor.',
  },
  tod5InteriorExposure: {
    type: 'float',
    default: -1,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Interior exposure offset',
    help: 'Extra exposure stops added to the global value on indoor pixels only. Use small positive values to keep interiors playable at night.',
  },
  tod5InteriorSaturation: {
    type: 'float',
    default: 1,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Interior saturation',
    help: 'Interior-only saturation multiplier for this time anchor.',
  },
  tod6GlobalTintR: {
    type: 'float',
    default: 3,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint R',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod6GlobalTintG: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint G',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod6GlobalTintB: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint B',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod6InteriorTintR: {
    type: 'float',
    default: 3,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint R',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod6InteriorTintG: {
    type: 'float',
    default: 1.39,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint G',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod6InteriorTintB: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint B',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod6Hour: {
    type: 'float',
    default: 18,
    min: 0,
    max: 24,
    step: 0.05,
    label: 'Clock hour',
    help: 'When the scene clock is near this anchor (18:00 by default). Anchors blend smoothly around the 24h cycle, including across midnight.',
  },
  tod6GlobalExposure: {
    type: 'float',
    default: -0.4,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Global exposure',
    help: 'Exposure in stops for outdoor and as the base for indoor pixels. Keep within +/-1 for natural looks; +/-3 is extreme.',
  },
  tod6GlobalSaturation: {
    type: 'float',
    default: 1,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Global saturation',
    help: 'Global saturation multiplier for this time anchor.',
  },
  tod6InteriorExposure: {
    type: 'float',
    default: -2,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Interior exposure offset',
    help: 'Extra exposure stops added to the global value on indoor pixels only. Use small positive values to keep interiors playable at night.',
  },
  tod6InteriorSaturation: {
    type: 'float',
    default: 1,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Interior saturation',
    help: 'Interior-only saturation multiplier for this time anchor.',
  },
  tod7GlobalTintR: {
    type: 'float',
    default: 0,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint R',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod7GlobalTintG: {
    type: 'float',
    default: 0,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint G',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod7GlobalTintB: {
    type: 'float',
    default: 3,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint B',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod7InteriorTintR: {
    type: 'float',
    default: 0.55,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint R',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod7InteriorTintG: {
    type: 'float',
    default: 0.71,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint G',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod7InteriorTintB: {
    type: 'float',
    default: 3,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint B',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod7Hour: {
    type: 'float',
    default: 21,
    min: 0,
    max: 24,
    step: 0.05,
    label: 'Clock hour',
    help: 'When the scene clock is near this anchor (21:00 by default). Anchors blend smoothly around the 24h cycle, including across midnight.',
  },
  tod7GlobalExposure: {
    type: 'float',
    default: -2,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Global exposure',
    help: 'Exposure in stops for outdoor and as the base for indoor pixels. Keep within +/-1 for natural looks; +/-3 is extreme.',
  },
  tod7GlobalSaturation: {
    type: 'float',
    default: 2,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Global saturation',
    help: 'Global saturation multiplier for this time anchor.',
  },
  tod7InteriorExposure: {
    type: 'float',
    default: 1,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Interior exposure offset',
    help: 'Extra exposure stops added to the global value on indoor pixels only. Use small positive values to keep interiors playable at night.',
  },
  tod7InteriorSaturation: {
    type: 'float',
    default: 1,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Interior saturation',
    help: 'Interior-only saturation multiplier for this time anchor.',
  },
  tod0GlobalTintR: {
    type: 'float',
    default: 0.5,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint R',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod0GlobalTintG: {
    type: 'float',
    default: 0.5,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint G',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod0GlobalTintB: {
    type: 'float',
    default: 2,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint B',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod0InteriorTintR: {
    type: 'float',
    default: 0.55,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint R',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod0InteriorTintG: {
    type: 'float',
    default: 0.71,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint G',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod0InteriorTintB: {
    type: 'float',
    default: 2.29,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint B',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod0Hour: {
    type: 'float',
    default: 0,
    min: 0,
    max: 24,
    step: 0.05,
    label: 'Clock hour',
    help: 'When the scene clock is near this anchor (00:00 by default). Anchors blend smoothly around the 24h cycle, including across midnight.',
  },
  tod0GlobalExposure: {
    type: 'float',
    default: -1,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Global exposure',
    help: 'Exposure in stops for outdoor and as the base for indoor pixels. Keep within +/-1 for natural looks; +/-3 is extreme.',
  },
  tod0GlobalSaturation: {
    type: 'float',
    default: 2,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Global saturation',
    help: 'Global saturation multiplier for this time anchor.',
  },
  tod0InteriorExposure: {
    type: 'float',
    default: 1,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Interior exposure offset',
    help: 'Extra exposure stops added to the global value on indoor pixels only. Use small positive values to keep interiors playable at night.',
  },
  tod0InteriorSaturation: {
    type: 'float',
    default: 2,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Interior saturation',
    help: 'Interior-only saturation multiplier for this time anchor.',
  },
  tod1GlobalTintR: {
    type: 'float',
    default: 0,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint R',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod1GlobalTintG: {
    type: 'float',
    default: 0,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint G',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod1GlobalTintB: {
    type: 'float',
    default: 3,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint B',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod1InteriorTintR: {
    type: 'float',
    default: 0.55,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint R',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod1InteriorTintG: {
    type: 'float',
    default: 0.71,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint G',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod1InteriorTintB: {
    type: 'float',
    default: 3,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint B',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod1Hour: {
    type: 'float',
    default: 3,
    min: 0,
    max: 24,
    step: 0.05,
    label: 'Clock hour',
    help: 'When the scene clock is near this anchor (03:00 by default). Anchors blend smoothly around the 24h cycle, including across midnight.',
  },
  tod1GlobalExposure: {
    type: 'float',
    default: -2,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Global exposure',
    help: 'Exposure in stops for outdoor and as the base for indoor pixels. Keep within +/-1 for natural looks; +/-3 is extreme.',
  },
  tod1GlobalSaturation: {
    type: 'float',
    default: 2,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Global saturation',
    help: 'Global saturation multiplier for this time anchor.',
  },
  tod1InteriorExposure: {
    type: 'float',
    default: 1,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Interior exposure offset',
    help: 'Extra exposure stops added to the global value on indoor pixels only. Use small positive values to keep interiors playable at night.',
  },
  tod1InteriorSaturation: {
    type: 'float',
    default: 0.98,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Interior saturation',
    help: 'Interior-only saturation multiplier for this time anchor.',
  },
  tod2GlobalTintR: {
    type: 'float',
    default: 3,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint R',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod2GlobalTintG: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint G',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod2GlobalTintB: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint B',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod2InteriorTintR: {
    type: 'float',
    default: 3,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint R',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod2InteriorTintG: {
    type: 'float',
    default: 1.39,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint G',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod2InteriorTintB: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint B',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod2Hour: {
    type: 'float',
    default: 6,
    min: 0,
    max: 24,
    step: 0.05,
    label: 'Clock hour',
    help: 'When the scene clock is near this anchor (06:00 by default). Anchors blend smoothly around the 24h cycle, including across midnight.',
  },
  tod2GlobalExposure: {
    type: 'float',
    default: -0.4,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Global exposure',
    help: 'Exposure in stops for outdoor and as the base for indoor pixels. Keep within +/-1 for natural looks; +/-3 is extreme.',
  },
  tod2GlobalSaturation: {
    type: 'float',
    default: 1,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Global saturation',
    help: 'Global saturation multiplier for this time anchor.',
  },
  tod2InteriorExposure: {
    type: 'float',
    default: -2,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Interior exposure offset',
    help: 'Extra exposure stops added to the global value on indoor pixels only. Use small positive values to keep interiors playable at night.',
  },
  tod2InteriorSaturation: {
    type: 'float',
    default: 1,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Interior saturation',
    help: 'Interior-only saturation multiplier for this time anchor.',
  },
  tod3GlobalTintR: {
    type: 'float',
    default: 1.2,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint R',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod3GlobalTintG: {
    type: 'float',
    default: 1.02,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint G',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod3GlobalTintB: {
    type: 'float',
    default: 1.06,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Global tint B',
    help: 'Global per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod3InteriorTintR: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint R',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod3InteriorTintG: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint G',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod3InteriorTintB: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Interior tint B',
    help: 'Interior per-channel multiply (1 = neutral, 0–3). Not a 0–255 colour.',
  },
  tod3Hour: {
    type: 'float',
    default: 9,
    min: 0,
    max: 24,
    step: 0.05,
    label: 'Clock hour',
    help: 'When the scene clock is near this anchor (09:00 by default). Anchors blend smoothly around the 24h cycle, including across midnight.',
  },
  tod3GlobalExposure: {
    type: 'float',
    default: 0.9,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Global exposure',
    help: 'Exposure in stops for outdoor and as the base for indoor pixels. Keep within +/-1 for natural looks; +/-3 is extreme.',
  },
  tod3GlobalSaturation: {
    type: 'float',
    default: 1,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Global saturation',
    help: 'Global saturation multiplier for this time anchor.',
  },
  tod3InteriorExposure: {
    type: 'float',
    default: -1,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Interior exposure offset',
    help: 'Extra exposure stops added to the global value on indoor pixels only. Use small positive values to keep interiors playable at night.',
  },
  tod3InteriorSaturation: {
    type: 'float',
    default: 1,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Interior saturation',
    help: 'Interior-only saturation multiplier for this time anchor.',
  },
  exposure: {
    type: 'float',
    default: 1,
    min: 0.25,
    max: 2,
    step: 0.01,
    label: 'Exposure',
    help: 'Final camera exposure multiplier before tone mapping. Dynamic exposure multiplies this at runtime.',
  },
  temperature: {
    type: 'float',
    default: 0,
    min: -1,
    max: 1,
    step: 0.01,
    label: 'Temperature',
    help: 'Warm (positive) vs cool (negative) white balance.',
  },
  tint: {
    type: 'float',
    default: 0,
    min: -1,
    max: 1,
    step: 0.01,
    label: 'Tint',
    help: 'Green vs magenta cast.',
  },
  brightness: {
    type: 'float',
    default: 0,
    min: -0.25,
    max: 0.25,
    step: 0.01,
    label: 'Brightness',
    help: 'Small constant offset after exposure and white balance. Prefer Exposure for physical brightness.',
  },
  contrast: {
    type: 'float',
    default: 1,
    min: 0.5,
    max: 1.6,
    step: 0.01,
    label: 'Contrast',
    help: 'Scales distance from 0.5 luma (1 = unchanged).',
  },
  saturation: {
    type: 'float',
    default: 1,
    min: 0,
    max: 1.6,
    step: 0.01,
    label: 'Saturation',
    help: '0 = grayscale, 1 ≈ natural, above 1 boosts color.',
  },
  vibrance: {
    type: 'float',
    default: 0,
    min: -1,
    max: 1,
    step: 0.01,
    label: 'Vibrance',
    help: 'Selective saturation boost (affects muted colors more).',
  },
  liftColor: {
    type: 'color',
    default: '#000000',
    space: 'linear',
    label: 'Lift',
    help: 'Shadow tint added before gain (scaled in shader).',
  },
  gammaColor: {
    type: 'color',
    default: '#808080',
    space: 'linear',
    label: 'Gamma',
    help: 'Per-channel gamma exponent (shader uses pow(rgb, 1/gamma); 1 = neutral).',
  },
  gainColor: {
    type: 'color',
    default: '#ffffff',
    space: 'linear',
    label: 'Gain',
    help: 'Per-channel multiply after lift.',
  },
  masterGamma: {
    type: 'float',
    default: 2,
    min: 0.1,
    max: 6,
    step: 0.01,
    label: 'Master gamma',
    help: 'Overall gamma after lift/gamma/gain (1 = neutral).',
  },
  toneMapping: {
    type: 'enum',
    default: 0,
    values: [0, 1, 2],
    valueLabels: {
      0: 'None',
      1: 'ACES Filmic',
      2: 'Reinhard',
    },
    label: 'Tone mapping',
    help: 'HDR → LDR curve. With the Linear HDR pipeline (Lighting/Sky output unclamped linear values), ACES is recommended. "None" leaves the merged HDR scene unmapped, which clips highlights on display.',
  },
  vignetteStrength: {
    type: 'float',
    default: 0,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Vignette strength',
    help: 'How much edges darken (0 = off).',
  },
  vignetteSoftness: {
    type: 'float',
    default: 0,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Vignette softness (reserved)',
    help: 'Reserved: uniform is updated but shader falloff is fixed today.',
  },
  grainStrength: {
    type: 'float',
    default: 0,
    min: 0,
    max: 0.5,
    step: 0.01,
    label: 'Grain strength',
    help: 'Animated film grain amplitude (0 = off).',
  },
  atmosphereEnabled: {
    type: 'bool',
    default: false,
    label: 'Enable outdoor atmosphere',
    help: 'Weather-aware golden hour / overcast offsets on sky-eligible outdoor pixels.',
  },
  intensity: {
    type: 'float',
    default: 1,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Outdoor atmosphere strength',
    help: 'Blend of procedural outdoor atmosphere on sky-visible pixels. Also exported as environment strength for water and weather-aware lighting.',
  },
  saturationBoost: {
    type: 'float',
    default: 0,
    min: -0.5,
    max: 0.5,
    step: 0.01,
    label: 'Sky color saturation',
  },
  vibranceBoost: {
    type: 'float',
    default: 0,
    min: -0.5,
    max: 0.5,
    step: 0.01,
    label: 'Sky color vibrance',
  },
  shadowGradePreserve: {
    type: 'float',
    default: 0.35,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Shadow preserve',
    help: 'Keeps shadowed outdoor pixels from full atmospheric recolor.',
  },
  sunriseHour: {
    type: 'float',
    default: 6,
    min: 0,
    max: 24,
    step: 0.05,
    label: 'Sunrise',
  },
  sunsetHour: {
    type: 'float',
    default: 18,
    min: 0,
    max: 24,
    step: 0.05,
    label: 'Sunset',
  },
  goldenHourWidth: {
    type: 'float',
    default: 1.3,
    min: 0.25,
    max: 6,
    step: 0.05,
    label: 'Golden Width',
  },
  goldenStrength: {
    type: 'float',
    default: 1,
    min: 0,
    max: 4,
    step: 0.01,
    label: 'Golden Strength',
  },
  goldenPower: {
    type: 'float',
    default: 1,
    min: 0.5,
    max: 3,
    step: 0.01,
    label: 'Golden Power',
  },
  goldenOutdoorRecolorStrength: {
    type: 'float',
    default: 0,
    min: 0,
    max: 4,
    step: 0.05,
    label: 'Golden Recolor',
  },
  goldenOutdoorRecolorColor: {
    type: 'color',
    default: '#ffffff',
    space: 'srgb',
    label: 'Golden Recolor Color',
  },
  nightFloor: {
    type: 'float',
    default: 0,
    min: 0,
    max: 0.5,
    step: 0.01,
    label: 'Night Floor',
  },
  analyticStrength: {
    type: 'float',
    default: 0.85,
    min: 0,
    max: 4,
    step: 0.01,
    label: 'Analytic Strength',
  },
  turbidity: {
    type: 'float',
    default: 0.22,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Turbidity',
  },
  rayleighStrength: {
    type: 'float',
    default: 0.63,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Rayleigh',
  },
  mieStrength: {
    type: 'float',
    default: 0.35,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Mie',
  },
  forwardScatter: {
    type: 'float',
    default: 0.3,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Forward Scatter',
  },
  weatherInfluence: {
    type: 'float',
    default: 0.67,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Weather Influence',
  },
  cloudToTurbidity: {
    type: 'float',
    default: 0.25,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Cloud→Turbidity',
  },
  precipToTurbidity: {
    type: 'float',
    default: 0.72,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Precip→Turbidity',
  },
  overcastDesaturate: {
    type: 'float',
    default: 0,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Overcast Desat',
  },
  overcastContrastReduce: {
    type: 'float',
    default: 0,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Overcast Contrast',
  },
  tempWarmAtHorizon: {
    type: 'float',
    default: 0,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Warm Horizon',
  },
  tempCoolAtNoon: {
    type: 'float',
    default: 0,
    min: -1,
    max: 0,
    step: 0.01,
    label: 'Cool Noon',
  },
  nightCoolBoost: {
    type: 'float',
    default: 0,
    min: -1,
    max: 0,
    step: 0.01,
    label: 'Night Cool',
  },
  goldenSaturationBoost: {
    type: 'float',
    default: 0,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Golden Sat',
  },
  nightSaturationFloor: {
    type: 'float',
    default: 0,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Night Sat Floor',
  },
  hazeLift: {
    type: 'float',
    default: 0,
    min: 0,
    max: 0.5,
    step: 0.01,
    label: 'Haze Lift',
  },
  hazeContrastLoss: {
    type: 'float',
    default: 0,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Haze Contrast',
  },
  autoIntensityEnabled: {
    type: 'bool',
    default: false,
    label: 'Auto Intensity',
  },
  autoIntensityStrength: {
    type: 'float',
    default: 1,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Auto Strength',
  },
  calendarDarknessBlend: {
    type: 'float',
    default: 1,
    min: 0,
    max: 1,
    step: 0.05,
    label: 'Master darkness blend',
  },
  dayNightGradePull: {
    type: 'float',
    default: 1,
    min: 0,
    max: 2.5,
    step: 0.05,
    label: 'Day/night color separation',
  },
  nightExtraDarkness: {
    type: 'float',
    default: 0,
    min: 0,
    max: 0.45,
    step: 0.01,
    label: 'Night color depth',
  },
};
