/**
 * MACHINE-HARVESTED from legacy/compositor-v2/effects/FloorDepthBlurEffect.js (V2 class FloorDepthBlurEffect), 2026-07-17.
 * Generated by tools/harvest-params.mjs — see that file for the transform rules.
 * Do not hand-edit the `SCHEMA`/`GROUPS` exports; re-run the harvest instead.
 */
export const SOURCE_CLASS = 'FloorDepthBlurEffect';
export const SOURCE_FILE = 'legacy/compositor-v2/effects/FloorDepthBlurEffect.js';
export const GROUPS = [
  {
    name: 'look',
    label: 'Look',
    type: 'folder',
    expanded: true,
    parameters: ['blurRadiusPx', 'itersPerDepth', 'maxIters'],
  },
];
export const SCHEMA = {
  enabled: {
    type: 'bool',
    default: false,
    label: 'enabled',
  },
  blurRadiusPx: {
    type: 'float',
    default: 6,
    min: 1,
    max: 30,
    step: 0.5,
    label: 'Blur per level (px)',
    help: 'How blurry each lower floor looks. Each floor down adds the same amount again.',
  },
  itersPerDepth: {
    type: 'int',
    default: 2,
    min: 1,
    max: 4,
    step: 1,
    label: 'Smoothness passes',
    help: 'More passes make the blur softer and less blocky; each pass is extra drawing work.',
  },
  maxIters: {
    type: 'int',
    default: 6,
    min: 2,
    max: 12,
    step: 1,
    label: 'Work limit',
    help: 'Stops the blur from running too many passes total, to protect frame rate.',
  },
};
