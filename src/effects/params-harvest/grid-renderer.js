/**
 * MACHINE-HARVESTED from legacy/scene/grid-renderer.js (V2 class GridRenderer), 2026-07-17.
 * Generated by tools/harvest-params.mjs — see that file for the transform rules.
 * Do not hand-edit the `SCHEMA`/`GROUPS` exports; re-run the harvest instead.
 */
export const SOURCE_CLASS = 'GridRenderer';
export const SOURCE_FILE = 'legacy/scene/grid-renderer.js';
export const GROUPS = [];
export const SCHEMA = {
  style: {
    type: 'enum',
    default: 'dashedLines',
    values: ['solidLines', 'dashedLines', 'dottedLines', 'squarePoints', 'diamondPoints', 'roundPoints'],
    valueLabels: {
      solidLines: 'Solid Lines',
      dashedLines: 'Dashed Lines',
      dottedLines: 'Dotted Lines',
      squarePoints: 'Square Points',
      diamondPoints: 'Diamond Points',
      roundPoints: 'Round Points',
    },
    label: 'Style (Override)',
  },
  useStyleOverride: {
    type: 'bool',
    default: true,
    label: 'Override Style',
  },
  thickness: {
    type: 'int',
    default: 2,
    min: 1,
    max: 10,
    step: 1,
    label: 'Thickness (Override)',
  },
  useThicknessOverride: {
    type: 'bool',
    default: true,
    label: 'Override Thickness',
  },
  colorOverride: {
    type: 'text',
    default: '#000000',
    label: 'Color (Override)',
  },
  useColorOverride: {
    type: 'bool',
    default: true,
    label: 'Override Color',
  },
  alphaOverride: {
    type: 'float',
    default: 0.15,
    min: 0,
    max: 1,
    step: 0.05,
    label: 'Opacity (Override)',
  },
  useAlphaOverride: {
    type: 'bool',
    default: true,
    label: 'Override Opacity',
  },
  ghostGridEnabled: {
    type: 'bool',
    default: true,
    label: 'Show Adjacent Floor Grids',
  },
  ghostGridAlphaScale: {
    type: 'float',
    default: 0.22,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Adjacent Grid Opacity Scale',
  },
  floorTintPresetsEnabled: {
    type: 'bool',
    default: true,
    label: 'Floor Color Tinting',
  },
};
