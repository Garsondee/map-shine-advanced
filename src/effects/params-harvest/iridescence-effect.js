/**
 * MACHINE-HARVESTED from legacy/compositor-v2/effects/IridescenceEffectV2.js (V2 class IridescenceEffectV2), 2026-07-17.
 * Generated by tools/harvest-params.mjs — see that file for the transform rules.
 * Do not hand-edit the `SCHEMA`/`GROUPS` exports; re-run the harvest instead.
 */
export const SOURCE_CLASS = 'IridescenceEffectV2';
export const SOURCE_FILE = 'legacy/compositor-v2/effects/IridescenceEffectV2.js';
export const GROUPS = [
  {
    name: 'mask-status-iridescence',
    type: 'mask-status',
    parameters: [],
  },
  {
    name: 'look',
    label: 'Look',
    type: 'folder',
    expanded: true,
    parameters: ['intensity', 'alpha'],
  },
  {
    name: 'motion',
    label: 'Motion & parallax',
    type: 'folder',
    expanded: true,
    parameters: ['flowSpeed', 'angle', 'parallaxStrength'],
  },
  {
    name: 'spectral',
    label: 'Spectral & lighting',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: ['noiseType', 'ignoreDarkness', 'colorCycleSpeed'],
  },
  {
    name: 'distortion',
    label: 'Distortion & noise',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: ['distortionStrength', 'noiseScale', 'phaseMult'],
  },
  {
    name: 'mask',
    label: 'Mask',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: ['maskThreshold', 'invertMask'],
  },
];
export const SCHEMA = {
  intensity: {
    type: 'float',
    default: 0.5,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Intensity',
    help: 'Strength of the iridescent color.',
  },
  alpha: {
    type: 'float',
    default: 0.5,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Opacity',
    help: 'Master alpha for the additive overlay.',
  },
  flowSpeed: {
    type: 'float',
    default: 1.5,
    min: 0,
    max: 5,
    step: 0.01,
    label: 'Flow speed',
    help: 'Screen-space scroll speed of the interference pattern.',
  },
  angle: {
    type: 'int',
    default: 0,
    min: 0,
    max: 360,
    step: 1,
    label: 'Angle',
    help: 'Flow direction in degrees.',
  },
  parallaxStrength: {
    type: 'float',
    default: 3,
    min: 0,
    max: 5,
    step: 0.01,
    label: 'Parallax strength',
    help: 'How much the pattern shifts with camera movement.',
  },
  noiseType: {
    type: 'enum',
    default: 0,
    values: [0, 1],
    valueLabels: {
      0: 'Liquid (smooth)',
      1: 'Glitter (grain)',
    },
    label: 'Noise type',
    help: 'Liquid = smoother bands; Glitter = sharper, grainier sparkle.',
  },
  ignoreDarkness: {
    type: 'float',
    default: 0.5,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Ignore darkness',
    help: 'Higher = keep iridescence visible when the scene is dark or night-tinted.',
  },
  colorCycleSpeed: {
    type: 'float',
    default: 0.1,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Color cycle speed',
    help: 'How fast hues shift over time.',
  },
  distortionStrength: {
    type: 'float',
    default: 0.92,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Distortion strength',
    help: 'UV warp from the mask into the noise field.',
  },
  noiseScale: {
    type: 'float',
    default: 0.68,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Noise scale',
    help: '0–1 UI mapped to internal noise frequency (higher = finer detail).',
  },
  phaseMult: {
    type: 'float',
    default: 4,
    min: 0.5,
    max: 6,
    step: 0.1,
    label: 'Phase multiplier',
    help: 'Density of interference fringes.',
  },
  maskThreshold: {
    type: 'float',
    default: 0.05,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Mask threshold',
    help: 'Minimum decoded mask strength to show iridescence; trims weak edges.',
  },
  invertMask: {
    type: 'bool',
    default: false,
    label: 'Invert mask',
    help: 'Off = brighter `_Iridescence` pixels = more shine (usual white-on-black paint). On = invert luminance (black = shine).',
  },
};
