/**
 * MACHINE-HARVESTED from legacy/compositor-v2/effects/LightingEffectV2.js (V2 class LightingEffectV2), 2026-07-17.
 * Generated by tools/harvest-params.mjs — see that file for the transform rules.
 * Do not hand-edit the `SCHEMA`/`GROUPS` exports; re-run the harvest instead.
 *
 * Excluded as LOCKED/DEPRECATED (numeric range with min === max — a real
 * writable param the source froze, e.g. "forced to 1.0", not a readout):
 *   composeToneExposure
 *
 * Excluded as SUPERSEDED FOSSILS — matches tools/verify-structure.mjs's
 * shadow/no-lift-no-combine wall (docs/planning/Light-and-Shadow.md): V2's
 * "lift" workaround for the wrong-noun shadow model, replaced by every light
 * carrying its own visibility term. Not harvested on purpose, not a gap:
 *   dynamicLightShadowOverrideStrength
 */
export const SOURCE_CLASS = 'LightingEffectV2';
export const SOURCE_FILE = 'legacy/compositor-v2/effects/LightingEffectV2.js';
export const GROUPS = [
  {
    name: 'ambientFloor',
    label: 'Ambient light (linear HDR)',
    type: 'folder',
    expanded: true,
    parameters: [
      'ambientDayScaleOutdoor',
      'ambientDayScaleIndoor',
      'ambientNightScaleOutdoor',
      'ambientNightScaleIndoor',
      'minIlluminationScale',
    ],
  },
  {
    name: 'ambientTwilight',
    label: 'Dawn / dusk (twilight)',
    type: 'folder',
    expanded: true,
    parameters: [
      'twilightDarkness',
      'twilightDayFloorOutdoor',
      'twilightDayFloorIndoor',
      'twilightMinLightKeepOutdoor',
      'twilightMinLightKeepIndoor',
    ],
  },
  {
    name: 'foundryLampsDay',
    label: 'Foundry lamps — Day',
    type: 'folder',
    expanded: true,
    parameters: ['foundryLightBrightnessDay', 'foundryLightColorBoostDay'],
  },
  {
    name: 'foundryLampsTwilight',
    label: 'Foundry lamps — Twilight',
    type: 'folder',
    expanded: true,
    parameters: ['foundryLightBrightnessTwilight', 'foundryLightColorBoostTwilight'],
  },
  {
    name: 'foundryLampsNight',
    label: 'Foundry lamps — Night',
    type: 'folder',
    expanded: true,
    parameters: ['foundryLightBrightnessNight', 'foundryLightColorBoostNight'],
  },
  {
    name: 'dynamicLuma',
    label: 'Player / shared gain',
    type: 'folder',
    expanded: false,
    parameters: ['lightIntensity'],
  },
  {
    name: 'windowCompose',
    label: 'Window glow (compose)',
    type: 'folder',
    expanded: false,
    parameters: [
      'windowEmissiveGain',
      'windowIndirectContrast',
      'windowWarmthTint',
      'windowAlbedoCoupling',
      'windowScreenSpill',
    ],
  },
  {
    name: 'pointFalloff',
    label: 'Point light falloff (half-life)',
    type: 'folder',
    expanded: true,
    parameters: [
      'falloffHalfInAtAtt0',
      'falloffHalfInAtAtt1',
      'falloffHalfOutAtAtt0',
      'falloffHalfOutAtAtt1',
      'falloffHalfMin',
      'falloffEdgeSoftBoostIn',
      'falloffEdgeSoftBoostOut',
      'falloffBrightNormInfluence',
      'falloffDimRingWeight',
      'falloffRimAAScale',
      'falloffAttCurvePower',
      'falloffRimBandAtAtt0',
      'falloffRimBandAtAtt1',
      'falloffExponent',
    ],
  },
  {
    name: 'colorationIntensity',
    label: 'Lamp colour (HDR radiance buffer)',
    type: 'folder',
    advanced: true,
    expanded: true,
    parameters: [
      'colorationMixScale',
      'colorationMaxMix',
      'colorationMixPower',
      'colorationStrength',
      'colorationReflectivity',
      'colorationSaturationBoost',
      'colorationFalloffStart',
      'colorationFalloffEnd',
      'colorationFalloffPower',
      'colorationEnergyGain',
    ],
  },
  {
    name: 'ambientShadowMix',
    label: 'Ambient occlusion from shadows',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'combinedShadowEffectStrength',
      'cloudShadowAmbientInfluence',
      'overheadShadowAmbientInfluence',
      'structuralSunAmbientOcclusion',
      'directStructuralOcclusionStrength',
    ],
  },
  {
    name: 'occlusion',
    label: 'Roof / floor occlusion',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'wallInsetPx',
      'wallPaddingPx',
      'restrictRoofScreenLightOcclusionToTopFloor',
      'upperFloorTransmissionEnabled',
      'upperFloorTransmissionStrength',
    ],
  },
  {
    name: 'darkness',
    label: 'Advanced darkness response',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: ['interiorDarkness', 'negativeDarknessStrength', 'darknessPunchGain'],
  },
  {
    name: 'lightAnim',
    label: 'Advanced light animation',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: ['lightAnimWindInfluence', 'lightAnimOutdoorPower'],
  },
  {
    name: 'perfTuning',
    label: 'Performance (internal RT scale)',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'internalLightResolutionScale',
      'internalWindowResolutionScale',
      'internalDarknessResolutionScale',
      'windowLightUseHalfFloat',
      'windowEmitCacheEnabled',
      'lightPrepassReuseEnabled',
    ],
  },
];
export const SCHEMA = {
  enabled: {
    type: 'bool',
    default: true,
    label: 'enabled',
  },
  globalIllumination: {
    type: 'float',
    default: 0,
    min: 0,
    max: 2,
    step: 0.1,
    label: 'Illumination scale (legacy)',
    help: 'Deprecated: use Day ambient and Night ambient. Kept so old module data still loads.',
  },
  ambientDayScale: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3.5,
    step: 0.05,
    label: 'Day ambient (legacy)',
    help: 'Deprecated: use Outdoor/Indoor day ambient. Loaded for old saves.',
  },
  ambientDayScaleOutdoor: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3.5,
    step: 0.05,
    label: 'Day ambient — outdoor',
    help: 'Scales Foundry ambient brightest on outdoor-classified pixels (porches, courtyards, sky reach).',
  },
  ambientDayScaleIndoor: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3.5,
    step: 0.05,
    label: 'Day ambient — indoor',
    help: 'Scales Foundry ambient brightest on indoor-classified pixels (rooms under roof capture).',
  },
  ambientNightScale: {
    type: 'float',
    default: 0.32,
    min: 0,
    max: 2,
    step: 0.05,
    label: 'Night ambient (legacy)',
    help: 'Deprecated: use Outdoor/Indoor night ambient.',
  },
  ambientNightScaleOutdoor: {
    type: 'float',
    default: 1,
    min: 0,
    max: 2,
    step: 0.05,
    label: 'Night ambient — outdoor',
    help: 'Scales Foundry ambientDarkness on outdoor pixels at high darkness.',
  },
  ambientNightScaleIndoor: {
    type: 'float',
    default: 1.1,
    min: 0,
    max: 2,
    step: 0.05,
    label: 'Night ambient — indoor',
    help: 'Scales Foundry ambientDarkness on indoor pixels; usually slightly lower than outdoor.',
  },
  minIlluminationScale: {
    type: 'float',
    default: 0,
    min: 0,
    max: 3,
    step: 0.05,
    label: 'Minimum light floor',
    help: 'Scales the darkest-scene safety floor so interiors never clip to pure black.',
  },
  twilightDarkness: {
    type: 'float',
    default: 0.32,
    min: 0,
    max: 0.85,
    step: 0.01,
    label: 'Twilight darkness',
    help: 'Calendar darkness at dawn and dusk. Lower = brighter golden hour; higher = sooner toward night.',
  },
  twilightDayFloorOutdoor: {
    type: 'float',
    default: 0.28,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Day floor — outdoor',
    help: 'Minimum day-ambient multiplier at sunrise/sunset on outdoor-classified pixels (porches, courtyards).',
  },
  twilightDayFloorIndoor: {
    type: 'float',
    default: 0.14,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Day floor — indoor',
    help: 'Minimum day-ambient at the horizon inside rooms (usually lower than outdoor).',
  },
  twilightMinLightKeepOutdoor: {
    type: 'float',
    default: 0.55,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Min-light keep — outdoor',
    help: 'Boosts the safety minimum-light floor during daylight hours outdoors.',
  },
  twilightMinLightKeepIndoor: {
    type: 'float',
    default: 0.3,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Min-light keep — indoor',
    help: 'Same floor boost for indoor pixels during daylight hours.',
  },
  foundryLightBrightnessDay: {
    type: 'float',
    default: 1,
    min: 0,
    max: 16,
    step: 0.05,
    label: 'Brightness boost',
    help: 'Foundry AmbientLight emission near solar noon. Does not affect candle/fire glow, window glow, or player torch.',
  },
  foundryLightColorBoostDay: {
    type: 'float',
    default: 1,
    min: 0,
    max: 8,
    step: 0.05,
    label: 'Colour boost',
    help: 'Foundry Color Intensity (gel) near solar noon on AmbientLight meshes only.',
  },
  foundryLightBrightnessTwilight: {
    type: 'float',
    default: 1,
    min: 0,
    max: 16,
    step: 0.05,
    label: 'Brightness boost',
    help: 'Foundry AmbientLight emission at dawn and dusk (horizon hours). Blends smoothly into day and night.',
  },
  foundryLightColorBoostTwilight: {
    type: 'float',
    default: 1,
    min: 0,
    max: 8,
    step: 0.05,
    label: 'Colour boost',
    help: 'Foundry Color Intensity (gel) at dawn and dusk on AmbientLight meshes only.',
  },
  foundryLightBrightnessNight: {
    type: 'float',
    default: 1,
    min: 0,
    max: 16,
    step: 0.05,
    label: 'Brightness boost',
    help: 'Foundry AmbientLight emission near midnight. Does not affect candle/fire glow, window glow, or player torch.',
  },
  foundryLightColorBoostNight: {
    type: 'float',
    default: 1,
    min: 0,
    max: 8,
    step: 0.05,
    label: 'Colour boost',
    help: 'Foundry Color Intensity (gel) near midnight on AmbientLight meshes only.',
  },
  foundryLightBrightness: {
    type: 'float',
    default: 1,
    min: 0,
    max: 16,
    step: 0.05,
    label: 'Brightness boost (legacy)',
    help: 'Deprecated: migrated to Foundry lamps — Day / Twilight / Night.',
  },
  foundryLightColorBoost: {
    type: 'float',
    default: 1,
    min: 0,
    max: 8,
    step: 0.05,
    label: 'Colour boost (legacy)',
    help: 'Deprecated: migrated to Foundry lamps — Day / Twilight / Night.',
  },
  lightIntensity: {
    type: 'float',
    default: 1,
    min: 0,
    max: 4,
    step: 0.05,
    label: 'Shared point gain',
    help: 'Base emission multiplier on torch/flash and Foundry lamps (`uComposeLightGain`) before `_lightRT`. Foundry lamps also use Brightness boost above. Candle glow may follow this when “Follow point light gain” is enabled on Candles.',
  },
  falloffHalfInAtAtt0: {
    type: 'float',
    default: 0.15,
    min: 0.01,
    max: 2,
    step: 0.005,
    label: 'Halving dist. (att 0, bright)',
    help: 'Bright-ring halving distance at the soft end (Foundry Attenuation 1). Larger = wider, softer pool.',
  },
  falloffHalfInAtAtt1: {
    type: 'float',
    default: 0.05,
    min: 0.005,
    max: 1,
    step: 0.005,
    label: 'Halving dist. (att 1, bright)',
    help: 'Bright-ring halving distance at the hard end (Foundry Attenuation 0). Smaller = tighter core.',
  },
  falloffHalfOutAtAtt0: {
    type: 'float',
    default: 0.65,
    min: 0.01,
    max: 2,
    step: 0.005,
    label: 'Halving dist. (att 0, dim)',
    help: 'Dim-ring hardness when attenuation is 0 (outer falloff to the photometric edge).',
  },
  falloffHalfOutAtAtt1: {
    type: 'float',
    default: 0.35,
    min: 0.005,
    max: 1,
    step: 0.005,
    label: 'Halving dist. (att 1, dim)',
    help: 'Dim-ring hardness at Attenuation 1 — should stay high enough to reach the dim radius before the edge fade.',
  },
  falloffHalfMin: {
    type: 'float',
    default: 0.02,
    min: 0.001,
    max: 0.5,
    step: 0.001,
    label: 'Min halving step',
    help: 'Floor when lerping halving distances (prevents extreme hardness).',
  },
  falloffEdgeSoftBoostIn: {
    type: 'float',
    default: 0.1,
    min: 0,
    max: 1.5,
    step: 0.01,
    label: 'Edge soft → bright ring',
    help: 'How much per-light edge softness widens the bright-ring halving distance.',
  },
  falloffEdgeSoftBoostOut: {
    type: 'float',
    default: 0.08,
    min: 0,
    max: 1.5,
    step: 0.01,
    label: 'Edge soft → dim ring',
    help: 'How much edge softness widens the dim-ring halving distance.',
  },
  falloffBrightNormInfluence: {
    type: 'float',
    default: 0.92,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Bright radius scale',
    help: 'Scales halving in the bright disk vs Foundry bright/dim radius ratio (larger bright radius → wider core).',
  },
  falloffDimRingWeight: {
    type: 'float',
    default: 1,
    min: 0.05,
    max: 2.5,
    step: 0.01,
    label: 'Dim ring weight',
    help: 'Weight of the outer (dim) ring vs bright (Foundry sums both zones). 1.0 matches core Foundry balance.',
  },
  falloffRimAAScale: {
    type: 'float',
    default: 0.38,
    min: 0.02,
    max: 2,
    step: 0.01,
    label: 'Rim AA width',
    help: 'Width of the anti-alias fade at the photometric outer edge (fraction of fade band).',
  },
  falloffAttCurvePower: {
    type: 'float',
    default: 1,
    min: 1,
    max: 4,
    step: 0.05,
    label: 'Attenuation curve',
    help: 'At 1.0, Foundry Attenuation 0 / 0.5 / 1 map evenly between the att=0 and att=1 halving distances (0.5 sits in the middle). Above 1.0 bends toward Attenuation 1 — at 2.5, Attenuation 0.5 looks almost like 0. Keep at 1.0 while tuning the halving-distance sliders.',
  },
  falloffRimBandAtAtt0: {
    type: 'float',
    default: 0.16,
    min: 0.01,
    max: 1,
    step: 0.01,
    label: 'Rim band (att 0)',
    help: 'Outer-edge AA band width at attenuation 0.',
  },
  falloffRimBandAtAtt1: {
    type: 'float',
    default: 0.08,
    min: 0.005,
    max: 0.5,
    step: 0.005,
    label: 'Rim band (att 1)',
    help: 'Outer-edge AA band width at attenuation 1.',
  },
  falloffExponent: {
    type: 'float',
    default: 2,
    min: 0.5,
    max: 8,
    step: 0.05,
    label: 'Falloff exponent bias',
    help: 'Slight modifier on legacy exponent uniform (2 ≈ neutral). Affects rim band + halving bias.',
  },
  colorationStrength: {
    type: 'float',
    default: 1,
    min: 1,
    max: 12,
    step: 0.1,
    label: 'Saturation boost gain',
    help: 'Extra lamp/albedo saturation that follows the soft light falloff (not the tight CI gel core). Does not change brightness halving. Leave at 1 unless you want punchier colour.',
  },
  colorationReflectivity: {
    type: 'float',
    default: 0,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Coloration reflectivity',
    help: '0 = vivid lamp hue on stone; 1 = tint follows albedo luma (weaker on dark cobble). Use 0 for torches.',
  },
  colorationMixScale: {
    type: 'float',
    default: 1,
    min: 0,
    max: 4,
    step: 0.05,
    label: 'CI buffer scale',
    help: 'Scales Foundry Color Intensity in the buffer (1.0 = full CI from the lamp; 0.4 = 40% max saturation).',
  },
  colorationMixPower: {
    type: 'float',
    default: 3.65,
    min: 0.1,
    max: 6,
    step: 0.05,
    label: 'CI curve power',
    help: 'Exponent on Foundry CI before compose (1 = linear; >1 widens gap between 0.5 and 1.0; <1 compresses mid CI).',
  },
  colorationMaxMix: {
    type: 'float',
    default: 0.69,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Max CI mix',
    help: 'Caps how much Foundry Color Intensity can push the buffer toward lamp hue [0..1].',
  },
  colorationEnergyGain: {
    type: 'float',
    default: 1,
    min: 0,
    max: 16,
    step: 0.1,
    label: 'Colour energy gain',
    help: 'Scales lamp energy before the colour penumbra smoothstep (extends colour toward the rim when >1).',
  },
  colorationFalloffStart: {
    type: 'float',
    default: 0,
    min: 0,
    max: 2,
    step: 0.005,
    label: 'Colour falloff start',
    help: 'Lamp-energy level where colour tint begins (smoothstep low edge).',
  },
  colorationFalloffEnd: {
    type: 'float',
    default: 0.01,
    min: 0.001,
    max: 4,
    step: 0.001,
    label: 'Colour falloff end',
    help: 'Lamp-energy level where CI reaches full strength. Lower this to 0.01 to keep lights colored all the way to their edges.',
  },
  colorationFalloffPower: {
    type: 'float',
    default: 1,
    min: 0.1,
    max: 12,
    step: 0.05,
    label: 'Colour falloff curve',
    help: 'Exponent on the colour penumbra mask (>1 = softer).',
  },
  colorationSaturation: {
    type: 'float',
    default: 0,
    min: -2,
    max: 4,
    step: 0.05,
    label: 'Lamp saturation',
    help: 'Pushes lamp chroma before tint (0 = buffer hue).',
  },
  colorationSaturationBoost: {
    type: 'float',
    default: 1.4,
    min: -2,
    max: 6,
    step: 0.05,
    label: 'Saturation boost',
    help: 'Pushes chroma after the lamp tint (0.5–1.5 typical). Does not change lamp brightness.',
  },
  colorationDarken: {
    type: 'float',
    default: 0,
    min: 0,
    max: 3,
    step: 0.05,
    label: 'Darken tinted',
    help: 'Scales down lit result where tint is active (deeper reds).',
  },
  colorationBrighten: {
    type: 'float',
    default: 0,
    min: -2,
    max: 4,
    step: 0.05,
    label: 'Brighten tinted',
    help: 'Adds peak multiplier where tint is active (can wash colour).',
  },
  colorationPeakPreserve: {
    type: 'float',
    default: 1,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Peak preserve',
    help: '1 = match white-light max channel after tint; 0 = allow peak to change.',
  },
  colorationHueShift: {
    type: 'float',
    default: 0,
    min: -1,
    max: 1,
    step: 0.01,
    label: 'Hue shift',
    help: 'Rotates lamp hue before tint (-1..1 = full wheel).',
  },
  colorationChromaCurve: {
    type: 'float',
    default: 1,
    min: 0.1,
    max: 8,
    step: 0.05,
    label: 'Chroma curve',
    help: 'Exponent on buffer color mix (higher = needs more CI before full tint).',
  },
  colorationAchromaticMix: {
    type: 'float',
    default: 0,
    min: 0,
    max: 1,
    step: 0.05,
    label: 'Neutral bleed',
    help: 'Blends color mix toward 1 (tint even when buffer chroma is low).',
  },
  composeToneMapping: {
    type: 'enum',
    default: 0,
    values: [0],
    valueLabels: {
      0: 'None',
    },
    label: 'Tone mapping (deprecated)',
    help: 'Deprecated: lighting pass always outputs linear HDR. Tone mapping is owned by Color Correction.',
  },
  combinedShadowEffectStrength: {
    type: 'float',
    default: 2,
    min: 1,
    max: 4,
    step: 0.05,
    label: 'Combined shadow strength',
    help: 'Amplifies unified shadow darkness on ambient only (1 = authored, 4 = very deep). Strong lights can still clear shadow override on structural paths.',
  },
  cloudShadowAmbientInfluence: {
    type: 'float',
    default: 1,
    min: 0,
    max: 1,
    step: 0.05,
    label: 'Cloud shadow on ambient',
    help: 'Reduces cloud (or combined) shadow on ambient only; dynamic lights stay full strength.',
  },
  overheadShadowAmbientInfluence: {
    type: 'float',
    default: 1,
    min: 0,
    max: 1,
    step: 0.05,
    label: 'Overhead shadow on ambient',
    help: 'Scales overhead tile shadow on ambient; torches and lamps still punch through.',
  },
  structuralSunAmbientOcclusion: {
    type: 'float',
    default: 1,
    min: 0,
    max: 1,
    step: 0.05,
    label: 'Structural shadow vs sky/day fill',
    help: 'For outdoor pixels under daylight: building + painted shadows stay darker than unified ambient lifts alone (minimum of unified shadow and structural occlusion). Dynamic shadow override still clears structural shadows near torches/player lights. Set to 0 for legacy behaviour.',
  },
  directStructuralOcclusionStrength: {
    type: 'float',
    default: 1,
    min: 0,
    max: 1,
    step: 0.05,
    label: 'Structural occlusion on HDR lights',
    help: 'Darkens the Foundry HDR light accumulation (ambient disks/torches/colour spill) wherever building+painted shadows are dark. Strong gameplay lights clear this via Structural shadow override. Set to 0 to restore fills that bypass structural shadows entirely.',
  },
  wallInsetPx: {
    type: 'float',
    default: 6,
    min: 0,
    max: 40,
    step: 0.5,
    label: 'Wall Inset (px)',
  },
  wallPaddingPx: {
    type: 'float',
    default: 2,
    min: 0,
    max: 12,
    step: 0.5,
    label: 'Wall Padding (px)',
    help: 'Expands blocking wall segments during light LOS raycasts. Reduces glow bleeding through thin or diagonal walls. Also applies to candle/fire glow pools.',
  },
  restrictRoofScreenLightOcclusionToTopFloor: {
    type: 'bool',
    default: true,
    label: 'Multi-floor: roof gate & building roof-cutout top floor only',
    help: 'When on (recommended for 2+ floors), Foundry lights use screen-space roof alpha only on the top floor so upstairs stamps do not cut lower-floor lights. Building shadows skip roof-cutout on the uppermost band when 2+ floors exist (that band’s map art is often on the roof layer and would otherwise suppress all building shadows). Single-floor maps unchanged. Turn off for legacy “always gate” on lights.',
  },
  upperFloorTransmissionEnabled: {
    type: 'bool',
    default: false,
    label: 'Upper Floor Through-Gaps',
  },
  upperFloorTransmissionStrength: {
    type: 'float',
    default: 0.6,
    min: 0,
    max: 2,
    step: 0.05,
    label: 'Upper Light Strength',
  },
  interiorDarkness: {
    type: 'float',
    default: 0,
    min: 0,
    max: 1.5,
    step: 0.05,
    label: 'Interior Darkness',
    help: 'Extra dim on mask-classified interiors (ambient only). Fades out automatically under Foundry / window light so torches and overlap regions do not stay muddy; specular in the scene buffer scales with total illumination.',
  },
  lightAnimWindInfluence: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3,
    step: 0.05,
    label: 'Wind Influence',
  },
  lightAnimOutdoorPower: {
    type: 'float',
    default: 2,
    min: 0,
    max: 6,
    step: 0.25,
    label: 'Outdoor Power',
  },
  negativeDarknessStrength: {
    type: 'float',
    default: 2,
    min: 0,
    max: 3,
    step: 0.1,
    label: 'Negative Darkness Strength',
  },
  darknessPunchGain: {
    type: 'float',
    default: 0,
    min: 0,
    max: 10,
    step: 0.1,
    label: 'Darkness Punch Gain',
  },
  internalLightResolutionScale: {
    type: 'float',
    default: 1,
    min: 0.25,
    max: 1,
    step: 0.05,
    label: 'Foundry lights RT scale',
    help: 'Internal resolution for `_lightRT` and stacked light buffers. Lower values reduce fill rate; point lights may soften slightly.',
  },
  internalWindowResolutionScale: {
    type: 'float',
    default: 0.5,
    min: 0.25,
    max: 1,
    step: 0.05,
    label: 'Window emit RT scale',
    help: 'Scales the scene-UV emit RT relative to compositor mask resolution (1.0 = native mask size). Do not cap to drawing-buffer size — that smears glow in compose.',
  },
  internalDarknessResolutionScale: {
    type: 'float',
    default: 1,
    min: 0.25,
    max: 1,
    step: 0.05,
    label: 'Darkness RT scale',
    help: 'Internal resolution for the darkness accumulation RT.',
  },
  windowLightUseHalfFloat: {
    type: 'bool',
    default: true,
    label: 'Window emit half-float',
    help: 'When false, emit RT uses 8-bit (less VRAM/bandwidth; may band on bright windows).',
  },
  windowEmitCacheEnabled: {
    type: 'bool',
    default: true,
    label: 'Window emit cache',
    help: 'Skips redundant full window-emit draws per floor when nothing changed (cross-frame). Disable if window glow ever looks stale.',
  },
  lightPrepassReuseEnabled: {
    type: 'bool',
    default: true,
    label: 'Light prepass reuse',
    help: 'Reuses the pre-bus Foundry light buffer at compose when floor/size/state match within the same frame. Disable if light rims flicker.',
  },
  windowEmissiveGain: {
    type: 'float',
    default: 0.55,
    min: 0,
    max: 4,
    step: 0.05,
    label: 'Window indirect gain',
    help: 'Scales HDR window spill in compose (emit strength is on Window Light → Intensity).',
  },
  windowIndirectContrast: {
    type: 'float',
    default: 1.2,
    min: 0.35,
    max: 1.2,
    step: 0.02,
    label: 'Window contrast',
    help: 'Lower = hotter window cores and softer penumbra (less flat grey at high intensity).',
  },
  windowWarmthTint: {
    type: 'float',
    default: 1,
    min: 0,
    max: 1,
    step: 0.02,
    label: 'Window warmth tint',
    help: 'How much emit colour tints nearby walls in compose (0 = neutral white spill).',
  },
  windowAlbedoCoupling: {
    type: 'float',
    default: 0,
    min: 0,
    max: 1.5,
    step: 0.02,
    label: 'Wall texture coupling',
    help: 'Scales indirect spill by wall albedo luma so masonry keeps texture while floors brighten.',
  },
  windowScreenSpill: {
    type: 'float',
    default: 0,
    min: 0,
    max: 2,
    step: 0.05,
    label: 'Window core glow',
    help: 'Small additive warm highlight on lit pixels at window cores (HDR-linear; not a full-frame multiply).',
  },
};
