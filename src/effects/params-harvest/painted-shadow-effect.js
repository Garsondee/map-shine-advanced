/**
 * MACHINE-HARVESTED from legacy/compositor-v2/effects/PaintedShadowEffectV2.js (V2 class PaintedShadowEffectV2), 2026-07-17.
 * Generated by tools/harvest-params.mjs — see that file for the transform rules.
 * Do not hand-edit the `SCHEMA`/`GROUPS` exports; re-run the harvest instead.
 */
export const SOURCE_CLASS = 'PaintedShadowEffectV2';
export const SOURCE_FILE = 'legacy/compositor-v2/effects/PaintedShadowEffectV2.js';
export const GROUPS = [
  {
    name: 'mask-status-handPaintedShadow',
    type: 'mask-status',
    parameters: [],
  },
  {
    name: 'main',
    label: 'Painted Shadows',
    type: 'inline',
    parameters: [
      'opacity',
      'shadowStrengthBoost',
      'length',
      'blurRadius',
      'contactShadowPreserve',
      'contactSharpBlendLow',
      'contactSharpBlendHigh',
      'shadowEdgeInflatePx',
      'resolutionScale',
    ],
  },
];
export const SCHEMA = {
  opacity: {
    type: 'float',
    default: 0.5,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Opacity',
  },
  shadowStrengthBoost: {
    type: 'float',
    default: 1,
    min: 1,
    max: 10,
    step: 0.05,
    label: 'Strength boost',
    help: 'Extra darkening beyond opacity (×1 matches older behavior; use up to ×10 when shadows look too faint).',
  },
  length: {
    type: 'float',
    default: 0.075,
    min: 0,
    max: 0.6,
    step: 0.005,
    label: 'Length',
  },
  blurRadius: {
    type: 'float',
    default: 4,
    min: 0,
    max: 4,
    step: 0.05,
    label: 'Blur',
  },
  contactShadowPreserve: {
    type: 'float',
    default: 1,
    min: 0,
    max: 1,
    step: 0.02,
    label: 'Contact preserve',
    help: 'Blurs outward without eating the caster edge: merges pre-blur strength where shadow is darkest, full blur where it fades.',
  },
  contactSharpBlendLow: {
    type: 'float',
    default: 0.04,
    min: 0,
    max: 0.35,
    step: 0.005,
    label: 'Contact blend (low)',
    help: 'Lower bound for where pre-blur strength starts to dominate. Raise slightly if fringe looks too crunchy.',
  },
  contactSharpBlendHigh: {
    type: 'float',
    default: 0.78,
    min: 0.2,
    max: 0.98,
    step: 0.01,
    label: 'Contact blend (high)',
    help: 'Upper bound toward full contact sharpness inside the silhouette. Lower to pull softness closer to walls.',
  },
  shadowEdgeInflatePx: {
    type: 'float',
    default: 1.25,
    min: 0,
    max: 8,
    step: 0.05,
    label: 'Edge inflate (px)',
    help: 'Expands painted shadow slightly in this pass’s pixel grid (often lower-res than canvas) so it tucks under assets and hides bright rim cracks. 0 = off.',
  },
  resolutionScale: {
    type: 'float',
    default: 2,
    min: 0.75,
    max: 2,
    step: 0.05,
    label: 'Resolution',
  },
};
