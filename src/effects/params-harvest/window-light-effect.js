/**
 * MACHINE-HARVESTED from legacy/compositor-v2/effects/WindowLightEffectV2.js (V2 class WindowLightEffectV2), 2026-07-17.
 * Generated by tools/harvest-params.mjs — see that file for the transform rules.
 * Do not hand-edit the `SCHEMA`/`GROUPS` exports; re-run the harvest instead.
 */
export const SOURCE_CLASS = 'WindowLightEffectV2';
export const SOURCE_FILE = 'legacy/compositor-v2/effects/WindowLightEffectV2.js';
export const GROUPS = [
  {
    name: 'mask-status-outdoors',
    type: 'mask-status',
    parameters: [],
  },
  {
    name: 'mask-status-windows',
    type: 'mask-status',
    parameters: [],
  },
  {
    name: 'lighting',
    label: 'Window Light',
    type: 'folder',
    expanded: true,
    parameters: ['intensity', 'falloff', 'color'],
  },
  {
    name: 'wl-glass-refraction',
    label: 'Glass Refraction',
    type: 'folder',
    expanded: false,
    parameters: [
      'glassRefractionEnabled',
      'rgbShiftAmount',
      'rgbShiftSoftness',
      'rgbShiftAngle',
      'rgbShiftSpread',
      'rgbShiftEdgeWeight',
      'rgbFringeSaturation',
      'rgbFringeBalance',
    ],
  },
  {
    name: 'wl-refraction-animation',
    label: 'Refraction Animation',
    type: 'folder',
    expanded: false,
    advanced: true,
    parameters: ['rgbShiftAnimate', 'rgbShiftAnimSpeed', 'rgbShiftAnimWobbleDeg'],
  },
  {
    name: 'wl-rain-glass',
    label: 'Rain on Glass',
    type: 'folder',
    expanded: false,
    parameters: [
      'rainGlassEnabled',
      'rainGlassWeatherResponse',
      'rainGlassStrength',
      'rainGlassScale',
      'rainGlassStretch',
      'rainGlassSpeed',
      'rainGlassSlopeInfluence',
      'rainGlassSlopeSamplePx',
      'rainGlassFallbackAngle',
    ],
  },
  {
    name: 'wl-sparkle-glint',
    label: 'Sparkle & Glint',
    type: 'folder',
    expanded: false,
    parameters: [
      'sparkleEnabled',
      'sparkleStrength',
      'sparkleSpeed',
      'sparkleScale',
      'sparkleThreshold',
      'sparkleEdgeBias',
      'sparkleColor',
      'specularBoost',
    ],
  },
  {
    name: 'wl-lightning-windows',
    label: 'Lightning on Windows',
    type: 'folder',
    expanded: false,
    advanced: true,
    parameters: [
      'lightningWindowEnabled',
      'lightningWindowIntensityBoost',
      'lightningWindowContrastBoost',
      'lightningWindowRgbBoost',
    ],
  },
  {
    name: 'environment',
    label: 'Environment',
    type: 'folder',
    expanded: false,
    parameters: ['cloudInfluence'],
  },
  {
    name: 'cloudShadows',
    label: 'Cloud Shadows',
    type: 'folder',
    expanded: false,
    parameters: ['cloudShadowContrast', 'cloudShadowBias', 'cloudShadowGamma', 'cloudShadowMinLight'],
  },
  {
    name: 'wl-tod-timeline',
    label: 'Time-of-day window light',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: ['todTimelineEnabled', 'useCameraGradeAnchorHours'],
  },
  {
    name: 'wl-tod-anchor-4',
    label: 'Noon (~12:00)',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'tod4Hour',
      'tod4IntensityScale',
      'tod4Exposure',
      'tod4Saturation',
      'tod4TintR',
      'tod4TintG',
      'tod4TintB',
    ],
  },
  {
    name: 'wl-tod-anchor-5',
    label: 'Afternoon (~15:00)',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'tod5Hour',
      'tod5IntensityScale',
      'tod5Exposure',
      'tod5Saturation',
      'tod5TintR',
      'tod5TintG',
      'tod5TintB',
    ],
  },
  {
    name: 'wl-tod-anchor-6',
    label: 'Dusk (~18:00)',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'tod6Hour',
      'tod6IntensityScale',
      'tod6Exposure',
      'tod6Saturation',
      'tod6TintR',
      'tod6TintG',
      'tod6TintB',
    ],
  },
  {
    name: 'wl-tod-anchor-7',
    label: 'Night (~21:00)',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'tod7Hour',
      'tod7IntensityScale',
      'tod7Exposure',
      'tod7Saturation',
      'tod7TintR',
      'tod7TintG',
      'tod7TintB',
    ],
  },
  {
    name: 'wl-tod-anchor-0',
    label: 'Midnight (~00:00)',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'tod0Hour',
      'tod0IntensityScale',
      'tod0Exposure',
      'tod0Saturation',
      'tod0TintR',
      'tod0TintG',
      'tod0TintB',
    ],
  },
  {
    name: 'wl-tod-anchor-1',
    label: 'Pre-dawn (~03:00)',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'tod1Hour',
      'tod1IntensityScale',
      'tod1Exposure',
      'tod1Saturation',
      'tod1TintR',
      'tod1TintG',
      'tod1TintB',
    ],
  },
  {
    name: 'wl-tod-anchor-2',
    label: 'Dawn (~06:00)',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'tod2Hour',
      'tod2IntensityScale',
      'tod2Exposure',
      'tod2Saturation',
      'tod2TintR',
      'tod2TintG',
      'tod2TintB',
    ],
  },
  {
    name: 'wl-tod-anchor-3',
    label: 'Morning (~09:00)',
    type: 'folder',
    advanced: true,
    expanded: false,
    parameters: [
      'tod3Hour',
      'tod3IntensityScale',
      'tod3Exposure',
      'tod3Saturation',
      'tod3TintR',
      'tod3TintG',
      'tod3TintB',
    ],
  },
];
export const SCHEMA = {
  hasWindowMask: {
    type: 'bool',
    default: true,
    label: 'hasWindowMask',
  },
  intensity: {
    type: 'float',
    default: 1,
    min: 0,
    max: 20,
    step: 0.05,
    label: 'Intensity',
    help: 'Window glow strength — multiplicative brighten (1 + light), not flat overlay.',
  },
  falloff: {
    type: 'float',
    default: 1,
    min: 0.5,
    max: 5,
    step: 0.05,
    label: 'Falloff (Gamma)',
  },
  color: {
    type: 'color',
    default: '#ffffff',
    space: 'srgb',
    label: 'Light Color',
  },
  cloudInfluence: {
    type: 'float',
    default: 1,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Cloud Dimming',
    help: 'How much overcast weather and cloud shadows dim window glow (0 = ignore clouds).',
  },
  cloudShadowContrast: {
    type: 'float',
    default: 4,
    min: 0,
    max: 4,
    step: 0.01,
    label: 'Shadow Contrast',
  },
  cloudShadowBias: {
    type: 'float',
    default: -1,
    min: -1,
    max: 1,
    step: 0.01,
    label: 'Shadow Bias',
  },
  cloudShadowGamma: {
    type: 'float',
    default: 4,
    min: 0.1,
    max: 4,
    step: 0.01,
    label: 'Shadow Gamma',
  },
  cloudShadowMinLight: {
    type: 'float',
    default: 0,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Min Light',
  },
  glassRefractionEnabled: {
    type: 'bool',
    default: true,
    label: 'Glass Refraction',
    help: 'Per-channel mask RGB shift for prismatic window fringes. Set RGB Shift to 0 to disable fringes while leaving this on.',
  },
  rgbShiftAmount: {
    type: 'float',
    default: 12.89,
    min: 0,
    max: 16,
    step: 0.01,
    label: 'RGB Shift (px)',
    help: 'Chromatic offset in mask texels along the shift angle.',
  },
  rgbShiftSoftness: {
    type: 'float',
    default: 0,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'RGB Shift Softness',
    help: 'Blends prismatic fringes toward neutral window glow (0 = full chromatic split, 1 = soft/monochrome).',
  },
  rgbShiftAngle: {
    type: 'int',
    default: 30,
    min: 0,
    max: 360,
    step: 1,
    label: 'Shift Angle (deg)',
  },
  rgbShiftSpread: {
    type: 'float',
    default: 0.46,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Spectral Spread',
    help: 'Widens R vs B separation (0 = symmetric).',
  },
  rgbShiftEdgeWeight: {
    type: 'float',
    default: 1,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Edge Fringe Weight',
    help: '1 = chromatic shift strongest on mask edges (pane borders).',
  },
  rgbFringeSaturation: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Fringe Saturation',
  },
  rgbFringeBalance: {
    type: 'color',
    default: '#ffffff',
    space: 'srgb',
    label: 'Fringe RGB Balance',
    help: 'Per-channel multiplier on chromatic fringe before falloff.',
  },
  rgbShiftAnimate: {
    type: 'bool',
    default: true,
    label: 'Animate Refraction',
  },
  rgbShiftAnimSpeed: {
    type: 'float',
    default: 0.45,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Animation Speed',
  },
  rgbShiftAnimWobbleDeg: {
    type: 'float',
    default: 28,
    min: 0,
    max: 90,
    step: 0.5,
    label: 'Angle Wobble (deg)',
    help: 'Peak swing of refraction angle while animated. Higher = more visible rainbow drift.',
  },
  rainGlassEnabled: {
    type: 'bool',
    default: true,
    label: 'Rain on Glass',
    help: 'Weather-driven procedural water streaks that refract window mask samples.',
  },
  rainGlassWeatherResponse: {
    type: 'float',
    default: 1,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Weather Response',
    help: 'Multiplier for precipitation intensity before it drives glass distortion.',
  },
  rainGlassStrength: {
    type: 'float',
    default: 0.045,
    min: 0,
    max: 0.15,
    step: 0.001,
    label: 'Distortion Strength',
    help: 'How far water droplets warp the window mask UVs.',
  },
  rainGlassScale: {
    type: 'int',
    default: 90,
    min: 8,
    max: 240,
    step: 1,
    label: 'Drop Scale',
    help: 'Higher values make smaller, denser droplets. Lower values make larger streaks.',
  },
  rainGlassStretch: {
    type: 'float',
    default: 0.25,
    min: 0.05,
    max: 1,
    step: 0.01,
    label: 'Streak Stretch',
    help: 'Lower values elongate drops along the flow direction.',
  },
  rainGlassSpeed: {
    type: 'float',
    default: 1.2,
    min: 0,
    max: 4,
    step: 0.01,
    label: 'Flow Speed',
    help: 'How quickly the procedural droplets slide across the pane.',
  },
  rainGlassSlopeInfluence: {
    type: 'float',
    default: 0.85,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Wall Slope Influence',
    help: '0 = fallback direction only; 1 = follow the local _Outdoors gradient around building edges.',
  },
  rainGlassSlopeSamplePx: {
    type: 'int',
    default: 42,
    min: 1,
    max: 160,
    step: 1,
    label: 'Slope Sample Radius',
    help: 'How far from each window pixel to sample the _Outdoors mask when estimating wall direction. Larger values smooth diagonal and thick-wall cases.',
  },
  rainGlassFallbackAngle: {
    type: 'int',
    default: 90,
    min: 0,
    max: 360,
    step: 1,
    label: 'Fallback Direction (deg)',
    help: 'Flow direction used when the _Outdoors/window gradient is too flat. 0 = left to right, 90 = top to bottom.',
  },
  specularBoost: {
    type: 'float',
    default: 2,
    min: 0,
    max: 5,
    step: 0.05,
    label: 'Specular Boost',
    help: 'Multiplies emit where _Specular mask is bright.',
  },
  sparkleEnabled: {
    type: 'bool',
    default: true,
    label: 'Sparkle Enabled',
  },
  sparkleStrength: {
    type: 'float',
    default: 1.35,
    min: 0,
    max: 5,
    step: 0.01,
    label: 'Sparkle Strength',
  },
  sparkleSpeed: {
    type: 'float',
    default: 1.2,
    min: 0,
    max: 4,
    step: 0.01,
    label: 'Sparkle Speed',
    help: 'Twinkle rate and how fast spawn locations shuffle (higher = quicker repopulation). Glints do not drift on the map.',
  },
  sparkleScale: {
    type: 'int',
    default: 38,
    min: 12,
    max: 120,
    step: 1,
    label: 'Sparkle Density',
    help: 'Glint cells across the visible camera view (not the whole map). Higher = more sparkles in what you see. Lower = fewer, larger gaps.',
  },
  sparkleThreshold: {
    type: 'float',
    default: 0.12,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Sparkle Core Threshold',
  },
  sparkleEdgeBias: {
    type: 'float',
    default: 0.72,
    min: 0,
    max: 1,
    step: 0.01,
    label: 'Sparkle Edge Bias',
    help: '0 = bright mask cores; 1 = pane edges.',
  },
  sparkleColor: {
    type: 'color',
    default: '#fffaeb',
    space: 'srgb',
    label: 'Sparkle Tint',
  },
  lightningWindowEnabled: {
    type: 'bool',
    default: true,
    label: 'Lightning Coupling',
  },
  lightningWindowIntensityBoost: {
    type: 'float',
    default: 5,
    min: 0,
    max: 5,
    step: 0.05,
    label: 'Flash Intensity Boost',
  },
  lightningWindowContrastBoost: {
    type: 'float',
    default: 4,
    min: 0,
    max: 4,
    step: 0.05,
    label: 'Flash Contrast Boost',
  },
  lightningWindowRgbBoost: {
    type: 'float',
    default: 3,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Flash RGB Shift Boost',
  },
  todTimelineEnabled: {
    type: 'bool',
    default: true,
    label: 'Enable time-of-day timeline',
    help: 'Blends eight clock anchors as Map Shine time advances. Adjusts window glow intensity, exposure, saturation, and tint per anchor.',
  },
  useCameraGradeAnchorHours: {
    type: 'bool',
    default: true,
    label: 'Use Camera Grade anchor hours',
    help: 'When enabled, blend points follow Camera Grade clock-hour sliders instead of the local hour sliders below.',
  },
  tod4TintR: {
    type: 'float',
    default: 0.8,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint R',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod4TintG: {
    type: 'float',
    default: 0.95,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint G',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod4TintB: {
    type: 'float',
    default: 1.35,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint B',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod4Hour: {
    type: 'float',
    default: 12,
    min: 0,
    max: 24,
    step: 0.05,
    label: 'Clock hour',
    help: 'When the scene clock is near this anchor (12:00 by default). Ignored when "Use Camera Grade anchor hours" is on.',
  },
  tod4IntensityScale: {
    type: 'float',
    default: 3,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Intensity scale',
    help: 'Window glow brightness multiplier at this anchor. Stacks with the master Intensity slider.',
  },
  tod4Exposure: {
    type: 'float',
    default: 0.15,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Exposure',
    help: 'Exposure in stops for window glow at this anchor.',
  },
  tod4Saturation: {
    type: 'float',
    default: 1.55,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Saturation',
    help: 'Chroma strength for window glow at this anchor (1 = neutral). Brightness is preserved — only hue richness changes.',
  },
  tod5TintR: {
    type: 'float',
    default: 1.3,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint R',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod5TintG: {
    type: 'float',
    default: 1.05,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint G',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod5TintB: {
    type: 'float',
    default: 1,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint B',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod5Hour: {
    type: 'float',
    default: 15,
    min: 0,
    max: 24,
    step: 0.05,
    label: 'Clock hour',
    help: 'When the scene clock is near this anchor (15:00 by default). Ignored when "Use Camera Grade anchor hours" is on.',
  },
  tod5IntensityScale: {
    type: 'float',
    default: 1.12,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Intensity scale',
    help: 'Window glow brightness multiplier at this anchor. Stacks with the master Intensity slider.',
  },
  tod5Exposure: {
    type: 'float',
    default: 0.12,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Exposure',
    help: 'Exposure in stops for window glow at this anchor.',
  },
  tod5Saturation: {
    type: 'float',
    default: 1.6,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Saturation',
    help: 'Chroma strength for window glow at this anchor (1 = neutral). Brightness is preserved — only hue richness changes.',
  },
  tod6TintR: {
    type: 'float',
    default: 3,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint R',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod6TintG: {
    type: 'float',
    default: 1.2,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint G',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod6TintB: {
    type: 'float',
    default: 0.9,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint B',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod6Hour: {
    type: 'float',
    default: 18,
    min: 0,
    max: 24,
    step: 0.05,
    label: 'Clock hour',
    help: 'When the scene clock is near this anchor (18:00 by default). Ignored when "Use Camera Grade anchor hours" is on.',
  },
  tod6IntensityScale: {
    type: 'float',
    default: 0.5,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Intensity scale',
    help: 'Window glow brightness multiplier at this anchor. Stacks with the master Intensity slider.',
  },
  tod6Exposure: {
    type: 'float',
    default: 0.3,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Exposure',
    help: 'Exposure in stops for window glow at this anchor.',
  },
  tod6Saturation: {
    type: 'float',
    default: 1.8,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Saturation',
    help: 'Chroma strength for window glow at this anchor (1 = neutral). Brightness is preserved — only hue richness changes.',
  },
  tod7TintR: {
    type: 'float',
    default: 0.65,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint R',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod7TintG: {
    type: 'float',
    default: 0.7,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint G',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod7TintB: {
    type: 'float',
    default: 2.15,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint B',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod7Hour: {
    type: 'float',
    default: 21,
    min: 0,
    max: 24,
    step: 0.05,
    label: 'Clock hour',
    help: 'When the scene clock is near this anchor (21:00 by default). Ignored when "Use Camera Grade anchor hours" is on.',
  },
  tod7IntensityScale: {
    type: 'float',
    default: 0.5,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Intensity scale',
    help: 'Window glow brightness multiplier at this anchor. Stacks with the master Intensity slider.',
  },
  tod7Exposure: {
    type: 'float',
    default: 0.1,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Exposure',
    help: 'Exposure in stops for window glow at this anchor.',
  },
  tod7Saturation: {
    type: 'float',
    default: 2,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Saturation',
    help: 'Chroma strength for window glow at this anchor (1 = neutral). Brightness is preserved — only hue richness changes.',
  },
  tod0TintR: {
    type: 'float',
    default: 0,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint R',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod0TintG: {
    type: 'float',
    default: 0.72,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint G',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod0TintB: {
    type: 'float',
    default: 3,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint B',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod0Hour: {
    type: 'float',
    default: 0,
    min: 0,
    max: 24,
    step: 0.05,
    label: 'Clock hour',
    help: 'When the scene clock is near this anchor (00:00 by default). Ignored when "Use Camera Grade anchor hours" is on.',
  },
  tod0IntensityScale: {
    type: 'float',
    default: 0.98,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Intensity scale',
    help: 'Window glow brightness multiplier at this anchor. Stacks with the master Intensity slider.',
  },
  tod0Exposure: {
    type: 'float',
    default: -1.04,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Exposure',
    help: 'Exposure in stops for window glow at this anchor.',
  },
  tod0Saturation: {
    type: 'float',
    default: 1.85,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Saturation',
    help: 'Chroma strength for window glow at this anchor (1 = neutral). Brightness is preserved — only hue richness changes.',
  },
  tod1TintR: {
    type: 'float',
    default: 1.4,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint R',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod1TintG: {
    type: 'float',
    default: 0.85,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint G',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod1TintB: {
    type: 'float',
    default: 2.3,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint B',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod1Hour: {
    type: 'float',
    default: 3,
    min: 0,
    max: 24,
    step: 0.05,
    label: 'Clock hour',
    help: 'When the scene clock is near this anchor (03:00 by default). Ignored when "Use Camera Grade anchor hours" is on.',
  },
  tod1IntensityScale: {
    type: 'float',
    default: 0.5,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Intensity scale',
    help: 'Window glow brightness multiplier at this anchor. Stacks with the master Intensity slider.',
  },
  tod1Exposure: {
    type: 'float',
    default: 0.05,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Exposure',
    help: 'Exposure in stops for window glow at this anchor.',
  },
  tod1Saturation: {
    type: 'float',
    default: 1.55,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Saturation',
    help: 'Chroma strength for window glow at this anchor (1 = neutral). Brightness is preserved — only hue richness changes.',
  },
  tod2TintR: {
    type: 'float',
    default: 3,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint R',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod2TintG: {
    type: 'float',
    default: 1.15,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint G',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod2TintB: {
    type: 'float',
    default: 0.85,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint B',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod2Hour: {
    type: 'float',
    default: 6,
    min: 0,
    max: 24,
    step: 0.05,
    label: 'Clock hour',
    help: 'When the scene clock is near this anchor (06:00 by default). Ignored when "Use Camera Grade anchor hours" is on.',
  },
  tod2IntensityScale: {
    type: 'float',
    default: 0.5,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Intensity scale',
    help: 'Window glow brightness multiplier at this anchor. Stacks with the master Intensity slider.',
  },
  tod2Exposure: {
    type: 'float',
    default: 0.25,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Exposure',
    help: 'Exposure in stops for window glow at this anchor.',
  },
  tod2Saturation: {
    type: 'float',
    default: 1.75,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Saturation',
    help: 'Chroma strength for window glow at this anchor (1 = neutral). Brightness is preserved — only hue richness changes.',
  },
  tod3TintR: {
    type: 'float',
    default: 1.45,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint R',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod3TintG: {
    type: 'float',
    default: 1.2,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint G',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod3TintB: {
    type: 'float',
    default: 0.95,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Tint B',
    help: 'Per-channel hue bias (1 = neutral, 0–3). Raise R / lower B for warmth; overall brightness is preserved. Not a 0–255 colour.',
  },
  tod3Hour: {
    type: 'float',
    default: 9,
    min: 0,
    max: 24,
    step: 0.05,
    label: 'Clock hour',
    help: 'When the scene clock is near this anchor (09:00 by default). Ignored when "Use Camera Grade anchor hours" is on.',
  },
  tod3IntensityScale: {
    type: 'float',
    default: 1.15,
    min: 0,
    max: 3,
    step: 0.01,
    label: 'Intensity scale',
    help: 'Window glow brightness multiplier at this anchor. Stacks with the master Intensity slider.',
  },
  tod3Exposure: {
    type: 'float',
    default: 0.2,
    min: -3,
    max: 3,
    step: 0.01,
    label: 'Exposure',
    help: 'Exposure in stops for window glow at this anchor.',
  },
  tod3Saturation: {
    type: 'float',
    default: 1.65,
    min: 0,
    max: 2,
    step: 0.01,
    label: 'Saturation',
    help: 'Chroma strength for window glow at this anchor (1 = neutral). Brightness is preserved — only hue richness changes.',
  },
};
